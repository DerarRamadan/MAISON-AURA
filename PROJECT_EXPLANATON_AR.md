# توثيق مشروع متجر العطور الفاخرة (Luxury Perfume Store)

## نظرة عامة
هذا المشروع عبارة عن تطبيق ويب متكامل لمتجر إلكتروني متخصص في بيع العطور الفاخرة. تم بناء المشروع باستخدام أحدث تقنيات الويب (React, TypeScript, Tailwind CSS) مع التركيز على تجربة مستخدم سلسة، تصميم عصري، وأداء عالي.

---

## 🛠 التقنيات المستخدمة

### 1. **React & TypeScript**
- تم استخدام **React** كإطار عمل لبناء واجهة المستخدم، مما يتيح بناء مكونات قابلة لإعادة الاستخدام (Components).
- تم استخدام **TypeScript** لإضافة نوع البيانات (Static Typing) مما يزيد من موثوقية الكود ويقلل من الأخطاء أثناء التطوير.

### 2. **State Management (إدارة الحالة)**
- **Zustand**: مكتبة خفيفة وسريعة لإدارة الحالة العامة للتطبيق. تم استخدامها لإنشاء "Stores" منفصلة لكل جزء من البيانات:
  - `useAuthStore`: لإدارة جلسة المستخدم وتسجيل الدخول.
  - `useCartStore` / `CartContext`: لإدارة سلة المشتريات.
  - `useProductStore`: لإدارة بيانات المنتجات (إضافة، تعديل، حذف).
  - `useOrderStore`: لإدارة الطلبات وحالاتها.
  - `useUserStore`: لإدارة بيانات العملاء.
  *ملاحظة: جميع المتاجر تستخدم `persist` middleware لحفظ البيانات في `localStorage` لضمان بقائها بعد تحديث الصفحة.*

### 3. **Styling & Animations (التصميم والتحركات)**
- **Tailwind CSS**: إطار عمل CSS يعتمد على الـ Utility classes لتصميم سريع ومتجاوب.
- **Framer Motion**: مكتبة قوية لإضافة تأثيرات حركية (Animations) سلسة، مثل ظهور العناصر عند التمرير (Scroll Reveal) وتفاعل الأزرار والقوائم.

### 4. **Routing (التوجيه)**
- **React Router DOM**: لإدارة التنقل بين الصفحات دون إعادة تحميل الصفحة (SPA).
- تم تقسيم المسارات إلى:
  - **مسارات عامة (Public Routes)**: الصفحة الرئيسية، المتجر، صفحة المنتج، الدفع.
  - **مسارات محمية (Protected Routes)**: لوحة تحكم المسؤول، والتي تتطلب تسجيل دخول.

### 5. **Internationalization (تعدد اللغات)**
- **i18next**: لدعم اللغتين العربية والإنجليزية.
- تم ضبط التطبيق ليدعم تغيير اتجاه الصفحة (RTL/LTR) تلقائيًا بناءً على اللغة المختارة.

---

## 📂 هيكلية المشروع

### 1. **Main Entry (`main.tsx` & `App.tsx`)**
- نقطة البداية للتطبيق.
- يتم هنا إعداد مزودي السياق (Providers) مثل `CartProvider` وتوجيه الصفحات.
- يتم التعامل مع تغيير اللغة واتجاه الصفحة.

### 2. **المتاجر (`src/store`)**
- تحتوي على منطق التعامل مع البيانات (Business Logic).
- مثال: في `useProductStore`، توجد دوال `addProduct`, `updateProduct`, `deleteProduct`.

### 3. **المكونات العامة (`src/components`)**
- **Layout**: الهيكل الأساسي للصفحات (Navbar + Content + Footer).
- **Navbar**: شريط التنقل العلوي، يدعم القائمة المنسدلة للجوال وتغيير اللغة.
- **ProductCard**: بطاقة عرض المنتج، تستخدم في المتجر والصفحة الرئيسية.
- **CartSidebar**: السلة الجانبية المنبثقة.

### 4. **الصفحات (`src/pages`)**

#### أ. الصفحة الرئيسية (`Home`)
- صفحة هبوط (Landing Page) بتصميم جذاب.
- تحتوي على أقسام: Hero (البانر)، BestSellers (الأكثر مبيعًا)، Collections (التصنيفات)، Story (قصتنا).

#### ب. المتجر (`Shop`)
- صفحة تعرض جميع المنتجات.
- **المزايا**:
  - فلترة المنتجات حسب الفئة (زهري، خشبي، شرقي...).
  - فلترة حسب السعر.
  - ترتيب المنتجات (الأعلى/الأقل سعرًا).

#### ج. تفاصيل المنتج (`ProductDetails`)
- تعرض صور المنتج، الوصف، السعر، والمنتجات المشابهة.
- زر لإضافة المنتج إلى السلة.

#### د. إتمام الطلب (`Checkout`)
- نموذج لإدخال بيانات العميل (الاسم، العنوان، الهاتف).
- التحقق من صحة البيانات (Validation).
- إنشاء طلب جديد وحفظه في النظام.

#### هـ. لوحة التحكم (`Admin`)
- **لوحة المعلومات (Dashboard)**: تعرض إحصائيات المبيعات، الطلبات الجديدة، ونمو العملاء.
- **إدارة المنتجات**: إضافة، تعديل، وحذف المنتجات.
- **إدارة الطلبات**: تغيير حالة الطلب (قيد المعالجة، تم الشحن، تم التوصيل).
- **إدارة المستخدمين**: إضافة مدراء وموظفين للنظام.
- **حماية**: لا يمكن الوصول إليها إلا بعد تسجيل الدخول.

---

## 🚀 كيفية التشغيل والتطوير

1. **تثبيت المكتبات**:
   ```bash
   npm install
   ```

2. **تشغيل خادم التطوير**:
   ```bash
   npm run dev
   ```

3. **بناء المشروع للإنتاج**:
   ```bash
   npm run build
   ```

---

## ✅ ملاحظات إضافية
- البيانات (المنتجات، المستخدمين، الطلبات) محفوظة مبدئيًا بشكل محلي (Local Storage) لمحاكاة قاعدة البيانات.
- التصميم متجاوب تمامًا (Responsive) ويعمل بكفاءة على الهواتف والأجهزة اللوحية.
- الكود موثق باللغة العربية لشرح وظيفة كل ملف ومكون رئيسي.
