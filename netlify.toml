[build]
  command = "npm run build" # الأمر المستخدم لبناء المشروع
  publish = "dist" # المجلد الذي سيتم نشره

[[redirects]]
  # توجيه جميع الطلبات إلى index.html لدعم React Router (SPA)
  from = "/*"
  to = "/index.html"
  status = 200
